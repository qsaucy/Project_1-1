#starts shell script in payara container :
sudo docker exec -it app_deployment /bin/bash


#in container bash, starts asadmin script :
cd /appserver/bin/
asadmin

sudo docker rmi -f $(sudo docker images -a -q)

